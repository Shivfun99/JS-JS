const fruits=["apple","boy","cat"];
const display=(fruit)=>{
    console.log(`${fruit} is a abcd`);
    
};
fruits.forEach(fruit=>display(fruit));
for(let i=0;i<3;i++){
    console.log(`${fruits[i]} is a abcd`);
    
}